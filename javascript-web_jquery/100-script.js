// Update text color of the HTML tag HEADER to red (#FF0000):
// imported from the head tag
document.addEventListener('DOMContentLoaded', function () {
  const header = document.querySelector('header');
  (header).style.color = '#FF0000';
});
