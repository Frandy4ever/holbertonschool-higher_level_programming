#!/usr/bin/python3
def delete_at(my_list=[], idx=0):
    # Check if the index is out of range or negative
    if idx >= 0 and idx < len(my_list):
       del my_list[idx]  # Return the original list unchanged

    return new_list
