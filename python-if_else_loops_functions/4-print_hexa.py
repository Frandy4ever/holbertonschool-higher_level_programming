#!/usr/bin/python3
# Loop through numbers from 0 to 98.
for number in range(0, 99):
    # Print the number in decimal and its hexadecimal representation.
    print("{} = {}".format(number, hex(number)))