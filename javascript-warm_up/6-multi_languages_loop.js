#!/usr/bin/node
// Prints values of array of strings
const randomLangs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
randomLangs.forEach(lang => console.log(lang));
// for (const lang of randomLangs) console.log(lang);
